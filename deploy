pipeline {
    agent any
	stages {
		stage('Deploy-App-QA') {
  	   steps {
              sh 'cat /tmp/env'
	   }
	   post { 
              always { 
                cleanWs() 
	      }
	   }
	}
  }
}
