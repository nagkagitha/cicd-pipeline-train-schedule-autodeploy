pipeline {
    agent any
	stages {
		stage('Deploy-App-QA') {
  	   steps {
              sh 'cat /tmp/inv'
	   }
	   post { 
              always { 
                cleanWs() 
	      }
	   }
	}
  }
}
